import itertools

def getCurrMin(x, y):
  tempMax = 0
  absval = abs(x-y)
  if tempMax < absval:
    
  

def findMaximum(arr, n):
    perms = set(itertools.permutations(arr, n))
    return perms
      


print(findMaximum([2, 3, 5], 2))
